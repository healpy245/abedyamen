; Applied on hosts that support per-directory PHP configuration (e.g. some Apache setups).
; Helps large menu text POST bodies. Adjust php.ini / pool config if these are ignored.
post_max_size = 64M
upload_max_filesize = 64M
max_input_vars = 10000
